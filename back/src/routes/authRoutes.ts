import { Router } from "express";

const authRouter:Router = Router();

authRouter.get('/algo', ()=>{})
authRouter.post('/algo', ()=>{})
authRouter.put('/algo', ()=>{})
authRouter.delete('/algo', ()=>{})

export default authRouter;